apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-mfe
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dashboard-mfe
  template:
    metadata:
      labels:
        app: dashboard-mfe
    spec:
      containers:
        - name: dashboard-mfe
          image: 192.168.101.30:8443/server-ui-v2-image/dashboard-mfe
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          volumeMounts:
            - name: tls-cert
              mountPath: /etc/nginx/certs
              readOnly: true
      volumes:
        - name: tls-cert
          secret:
            secretName: dashboard-mfe-tls

---
apiVersion: v1
kind: Service
metadata:
  name: dashboard-mfe
spec:
  type: NodePort
  selector:
    app: dashboard-mfe
  ports:
    - name: http
      port: 443
      targetPort: 443
      nodePort: 31112
